language: python
python:
  - "3.9"
  environment:
      - PYTHONPATH=.
install:
  - pip install -r requirements.txt
# command to run tests
script:
  - pytest  --pylint --cov
  environment:
      - PYTHONPATH=.

branches:
  only:
    - main
    - development

